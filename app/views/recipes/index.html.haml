%h1 Recipes
- @categories.each do |category|
  %h3= category.name
  .category.flex
    - category.recipes.each do |recipe|
      .recipe.max-w-sm.bg-white.p-4.m-4.rounded.overflow-hidden.shadow
        = image_tag 'burger.jpeg', class: 'w-full'
        -# = image_tag recipe.image, class: 'w-full' unless recipe.image.attachment.nil?
        .px-6.py-4
          %h3.mb-2= link_to recipe.title, recipe_path(recipe)
          .text-grey-darker= recipe.description[0..250] + '...'
          .px-6.py-4
            .flex.items-center.text-sm
              .mr-2 Posted by:
              = mail_to recipe.user.email
